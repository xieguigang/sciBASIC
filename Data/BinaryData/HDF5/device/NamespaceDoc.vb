Namespace device

    ''' <summary>
    ''' Low level HDF5 byte access: the binary and memory file readers, the global heap id and the
    ''' read helper primitives, together with the dataset readers for enum and variable length
    ''' datasets.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

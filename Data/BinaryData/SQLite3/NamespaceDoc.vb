''' <summary>
''' A pure managed SQLite3 database file toolkit: decodes the database header, B-tree pages, the
''' master table and column types and exposes tables and rows without the native SQLite engine.
''' </summary>
Friend Class NamespaceDoc
End Class

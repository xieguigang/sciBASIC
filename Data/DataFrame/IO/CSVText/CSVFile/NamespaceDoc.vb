Namespace IO.CSVFile

    ''' <summary>
    ''' The CSV file loader and row tokenizer: header parsing, row iteration, field tokenization and
    ''' streaming CSV reading.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

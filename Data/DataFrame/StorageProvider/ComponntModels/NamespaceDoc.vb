Namespace StorageProvider.ComponentModels

    ''' <summary>
    ''' The storage provider component model: the provider base, schema provider, row builder and
    ''' row writer, together with the reflection bridges for arrays, dictionaries, enums, fields and
    ''' key/value pairs.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

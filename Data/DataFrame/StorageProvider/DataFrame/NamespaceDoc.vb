Namespace StorageProvider

    ''' <summary>
    ''' The storage provider abstraction for data frames: the schema interfaces, the header schema
    ''' and the data frame resolver used to bind tables to object models.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

Namespace StorageProvider.Reflection

    ''' <summary>
    ''' Reflection based storage providers: the column and data attributes, metadata providers and
    ''' the type schema provider that map .NET types to table schemas.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

Namespace BigData

    ''' <summary>
    ''' 当进行一个非常大的系统的拟合的时候，所需要的内存会非常的多
    ''' 因为大部分的变量间的相关系数都不是零的话
    ''' 
    ''' 可能直接使用稀疏向量会反而造成内存的大量消耗
    ''' 在这里尝试使用16bit的半精度向量来减少内存的消耗
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace
Namespace CNN

    ''' <summary>
    ''' The convolutional neural network: the network model together with its data containers, layers,
    ''' loss layers and trainers.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

目前我正在使用vb.net语言开发一套基于gdi+图形代码的数据可视化框架，在这个框架中绘图的功能主要对标R语言中的ggplot程序包。

在这套数据可视化系统中，有一个样式主题系统，需要你尝试构建一套基于css样式定义的主题系统为我的绘图程序进行样式的配置。所上传的markdown文档为ggplot程序包中的theme函数的定义，目前我需要你基于vb.net语言为我编写一个能够涵盖ggplot程序包中的theme函数中的参数的主题对象。在这个使用vb.net代码所实现的主题系统中，这套系统会需要满足所上传的ggplot theme函数的文档中所描述的功能以及定义：

1. 在主题中定义根元素，其他的子元素会默认继承这些根元素的主题定义，同时子元素的样式也可以被单独自定义。
2. 以及实现一套自动布局计算系统，会尝试将css中所定义的不同单位（例如"cm","mm","pt","lines","char","null","npc","inch","px"）的大小值和距离值，在给定的画布环境中（主要涉及到画布大小，ppi大小），根据theme中的元素的定义，将theme对象中带有位置信息的绘图元素的布局信息转换为pixel单位的布局位置以及计算线条粗细，字号大小等信息为pixel单位

然后基于这个主题对象编写一个css样式的映射模块，在这个css样式映射模块中需要有两个公共函数，分别用于实现从css文件中解析出theme对象实例，以及将theme对象实例序列化为css文件
Namespace IL2Cuda

    ''' <summary>
    ''' IL to CUDA source translator: the CudaKernel and parameter/index attributes that mark .NET
    ''' methods as kernels, the .NET to CUDA type mapping table, and the IL emitter that translates
    ''' the marked method bodies into CUDA C source.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

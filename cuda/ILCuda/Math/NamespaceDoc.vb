Namespace Math

    ''' <summary>
    ''' CUDA math kernels exposed as managed wrappers: element-wise unary and binary operations,
    ''' generic reductions and a BLAS subset (GEMM/GEMV) running on the GPU.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

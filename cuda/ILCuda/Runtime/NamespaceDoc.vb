Namespace Runtime

    ''' <summary>
    ''' CUDA runtime management: device, context, stream, event, module and device/host memory
    ''' objects, JIT compilation of kernels through NVRTC, cubin inspection, kernel catalog and
    ''' launch configuration, plus the high level CudaEngine and environment diagnostics.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

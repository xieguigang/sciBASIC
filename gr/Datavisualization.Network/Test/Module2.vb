Imports Microsoft.VisualBasic.Mathematical.Matrix

Module Module2

    'Public Function rank2(M As GeneralMatrix, d#, v_quadratic_error#)
    '    Dim N = size(M, 2) ' N Is equal To either dimension Of M And the number Of documents
    '    Dim v = rand(N, 1)
    '    v = v / v.Norm1  ' This Is now L1, Not L2
    '    Dim last_v = ones(N, 1) * inf
    '    M_hat = (d.* M) + (((1 - d) / N).* ones(N, N))

    '    Do While (norm(v - last_v, 2) > v_quadratic_error)
    '        last_v = v
    '        v = M_hat * v
    '        ' removed the L2 norm of the iterated PR
    '    Loop
    'End Function

    Sub Main()



    End Sub

End Module

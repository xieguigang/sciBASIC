Namespace Driver

    ''' <summary>
    ''' The graphics driver abstraction: the IGraphics driver contract and its factory, device
    ''' descriptions, the internal canvas and image synthesis, together with the graphics data
    ''' models (bitmap, SVG, PostScript and WMF) and the JPEG metadata helpers.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

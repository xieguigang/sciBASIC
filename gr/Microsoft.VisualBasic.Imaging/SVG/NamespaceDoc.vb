Namespace SVG

    ''' <summary>
    ''' The SVG output driver: the SVG writer and renderer, the SVG data layers, and the geometry
    ''' elements and transforms.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

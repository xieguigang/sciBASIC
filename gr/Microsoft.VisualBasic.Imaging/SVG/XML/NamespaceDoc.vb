Namespace SVG.XML

    ''' <summary>
    ''' The SVG XML element model: the SVG element base type with default attributes, styling, fonts
    ''' and metadata, the basic shape elements (rect, circle, ellipse, line, path, polygon,
    ''' polyline, image, text, title) and the container elements (document, group, defs, marker,
    ''' symbol, clip path, switch, use and foreign object).
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

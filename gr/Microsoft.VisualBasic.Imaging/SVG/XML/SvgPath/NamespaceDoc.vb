Namespace SVG.PathHelper

    ''' <summary>
    ''' SVG path data handling: the path command model, the path interpreter and model builder
    ''' together with the individual path commands (A, C, H, L, M, Q, S, T, V and Z).
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

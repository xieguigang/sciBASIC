Namespace BSON

    ''' <summary>
    ''' BSON binary encoding and decoding: the BSON document/value model, the binary encoder and
    ''' decoder, and the extended BSON value types such as ObjectId.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

Namespace LenientJson

    ''' <summary>
    ''' A lenient JSON parser that tolerates and repairs malformed JSON, such as text produced by
    ''' large language models, by tracking the string context while scanning.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

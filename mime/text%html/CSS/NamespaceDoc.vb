Namespace CSS

    ''' <summary>
    ''' The CSS style environment: CSS layers, length units and typed CSS values, together with
    ''' the element style models (font style, fill, stroke, padding and size) used to resolve
    ''' styles.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

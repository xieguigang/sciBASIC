Namespace Language

    ''' <summary>
    ''' The HTML parser: the tokenizer, token types, escaping rules and the parser that builds the
    ''' document object model from HTML source text.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

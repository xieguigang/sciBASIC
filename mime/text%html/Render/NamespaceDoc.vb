Namespace Render

    ''' <summary>
    ''' The lightweight HTML renderer: the render tree model (tags, text strings and containers)
    ''' and the renderer that draws an HTML document.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

''' <summary>
''' A YAML parser for the sciBASIC# framework that builds the JsonElement object model from block
''' mappings, sequences, flow collections, block scalars, anchors, aliases and comments, plus a
''' lightweight markdown front matter reader.
''' </summary>
Friend Class NamespaceDoc
End Class

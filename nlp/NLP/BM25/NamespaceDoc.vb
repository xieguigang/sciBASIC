Namespace BM25

    ''' <summary>
    ''' Okapi BM25 ranked retrieval engine: indexes documents with per-term contributions and
    ''' selectable IDF variants, then scores and returns the best matching documents for a query.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace

Namespace ChineseTokenizer.HuggingFace

    ''' <summary>
    ''' HuggingFace compatible tokenizer implementation: BPE, WordPiece and Unigram models with the
    ''' normalizer, pre-tokenizer, post-processor and decoder pipeline, plus the tokenizer JSON
    ''' configuration reader.
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace
